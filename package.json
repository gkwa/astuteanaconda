{
  "name": "astuteanaconda",
  "version": "1.0.0",
  "description": "Chrome extension to extract products using SocialSparrow API and automatically send to DynamoDB",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "watch": "vite build --watch",
    "lint": "eslint src/**/*.js"
  },
  "devDependencies": {
    "eslint": "^9.23.0",
    "vite": "^5.4.15",
    "vite-plugin-static-copy": "^1.0.6"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "socialsparrow": "^0.3.8"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"],
    "ignoredBuiltDependencies": ["esbuild"]
  }
}
