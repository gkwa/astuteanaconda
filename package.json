{
  "name": "astuteanaconda",
  "version": "1.0.0",
  "description": "Chrome extension to extract products using SocialSparrow API and save to DynamoDB",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node build.mjs",
    "watch": "vite build --watch",
    "lint": "eslint src/**/*.js"
  },
  "devDependencies": {
    "eslint": "^9.24.0",
    "vite": "^6.2.6",
    "vite-plugin-static-copy": "^2.3.1"
  },
  "dependencies": {
    "socialsparrow": "^0.3.25"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ],
    "ignoredBuiltDependencies": [
      "esbuild"
    ],
    "overrides": {}
  }
}
